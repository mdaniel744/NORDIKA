# Legacy Vite frontend

This directory contains the pre-migration React/Vite implementation for traceability. It is not part of the active build and its dependency stack has been removed.

The production application lives in `src/app` and runs on Next.js. Do not restore this archive into the active source tree unless intentionally investigating historical behavior.
